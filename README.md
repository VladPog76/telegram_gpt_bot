
# 🤖 Telegram Bot з ChatGPT

Багатофункціональний Telegram-бот з інтеграцією OpenAI ChatGPT, підтримкою голосових повідомлень, перекладачем та роботою через проксі-сервер.

## 📋 Можливості

### Обов'язкові функції:
- ✅ **ChatGPT інтерфейс** (`/gpt`) - задавай питання ChatGPT текстом або голосом
- ✅ **Випадкові факти** (`/random`) - отримуй цікаві факти
- ✅ **Діалог з особистістю** (`/talk`) - спілкуйся з Ейнштейном, Шекспіром, Ілоном Маском та Стівом Джобсом
- ✅ **Квіз** (`/quiz`) - вікторина на 6 тем з підрахунком балів

### Додаткові функції:
- 🎤 **Голосовий ChatGPT** - надішли голосове повідомлення, отримай голосову відповідь
- 🌍 **Перекладач** (`/translate`) - переклад тексту та голосових повідомлень на 6 мов

### Технічні особливості:
- 🔒 **Підтримка проксі** - робота через проксі-сервер для спільного використання токена
- 📝 **Логування** - детальні логи всіх операцій
- 🛡️ **Відмовостійкість** - обробка помилок API та мережі
- 🎨 **Модульна архітектура** - чистий та підтримуваний код

## 🚀 Встановлення та запуск

### 1. Клонуй репозиторій
```bash
git clone https://github.com/твій-username/telegram_gpt_bot.git
cd telegram_gpt_bot
```

### 2. Створи віртуальне оточення
```bash
python -m venv .venv

# Windows
.venv\Scripts\activate

# Linux/Mac
source .venv/bin/activate
```

### 3. Встанови залежності
```bash
pip install -r requirements.txt
```

### 4. Налаштуй токени та проксі
Створи файл `.env` у корені проєкту:
```env
TELEGRAM_TOKEN=твій_токен_від_BotFather
OPENAI_TOKEN=твій_токен_OpenAI
PROXY=http://18.199.183.77:49232
```

**Примітка:** Рядок `PROXY` опціональний. Якщо працюєш локально без обмежень - можеш його не вказувати або закоментувати:
```env
# PROXY=http://18.199.183.77:49232
```

**Як отримати токени:**
- **Telegram:** Напиши @BotFather в Telegram → `/newbot` → дотримуйся інструкцій
- **OpenAI:** Зареєструйся на https://platform.openai.com → API keys → Create new key
- **Проксі:** Отримай адресу проксі-сервера від координатора проєкту (якщо працюєш в команді)

### 5. Налаштуй команди бота в BotFather (опціонально)

Відкрий @BotFather в Telegram і відправ:
```
/mybots
→ Вибери свого бота
→ Edit Bot
→ Edit Commands
```

Вставте цей список:
```
start - Головне меню
help - Довідка по командам
gpt - Задати питання ChatGPT
random - Випадковий цікавий факт
talk - Поговорити з відомою особистістю
quiz - Зіграти у вікторину
translate - Перекладач тексту та голосу
```

### 6. Запусти бота
```bash
python bot.py
```

Ти побачиш:
```
2025-10-22 16:00:00,123 - __main__ - INFO - Запуск бота...
2025-10-22 16:00:00,124 - __main__ - INFO - Використовується проксі: http://18.199.183.77:49232
2025-10-22 16:00:00,125 - utils.openai_helper - INFO - OpenAI використовує проксі: http://18.199.183.77:49232
2025-10-22 16:00:01,456 - __main__ - INFO - Бот успішно запущений і очікує команд.
```

## 📦 Вимоги

- Python 3.13+
- Токен Telegram бота (отримати у @BotFather)
- API ключ OpenAI
- (Опціонально) Адреса проксі-сервера для спільної роботи

## 🛠️ Технології

- `python-telegram-bot==22.5` - взаємодія з Telegram API
- `openai==2.4.0` - інтеграція з ChatGPT, Whisper, TTS
- `python-dotenv==1.1.1` - управління змінними оточення
- `httpx==0.27.0` - HTTP клієнт з підтримкою проксі

## 📁 Структура проєкту
```
telegram_gpt_bot/
├── bot.py                      # Головний файл - ініціалізація та запуск
├── config.py                   # Конфігурація та завантаження токенів
├── handlers/                   # Обробники команд
│   ├── __init__.py
│   ├── basic.py               # /start, /help
│   ├── gpt_handler.py         # /gpt - ChatGPT інтерфейс
│   ├── random_handler.py      # /random - випадкові факти
│   ├── talk_handler.py        # /talk - діалог з особистостями
│   ├── quiz_handler.py        # /quiz - вікторина
│   ├── translate_handler.py   # /translate - перекладач
│   └── voice_handler.py       # Обробка голосових повідомлень
├── utils/                      # Допоміжні модулі
│   ├── __init__.py
│   ├── openai_helper.py       # Функції для роботи з OpenAI API
│   └── constants.py           # Константи та словники
├── images/                     # Зображення для команд
│   ├── gpt.jpg
│   ├── random.jpg
│   ├── talk.jpg
│   ├── quiz.jpg
│   └── translate.jpg
├── temp/                       # Тимчасові файли (створюється автоматично)
├── .env                        # Токени та проксі (не в Git!)
├── .gitignore                  # Виключення для Git
├── requirements.txt            # Залежності Python
├── bot.log                     # Логи бота (створюється автоматично)
└── README.md                   # Документація
```

## 🎮 Команди бота

| Команда | Опис |
|---------|------|
| `/start` | Головне меню з описом усіх команд |
| `/help` | Довідка щодо використання бота |
| `/gpt` | Задати питання ChatGPT (текст або голос) |
| `/random` | Отримати випадковий цікавий факт |
| `/talk` | Поговорити з відомою особистістю |
| `/quiz` | Зіграти у вікторину |
| `/translate` | Перекласти текст або голосове повідомлення |

## 🔌 Робота через проксі

### Навіщо потрібен проксі?

Коли багато людей використовують один токен OpenAI, API може обмежити кількість запитів з однієї IP-адреси. Проксі-сервер вирішує цю проблему.

### Схема роботи:
```
Твій бот → Проксі-сервер → OpenAI API
```

OpenAI бачить IP проксі-сервера, а не твою справжню IP.

### Налаштування:

**У файлі `.env` додай:**
```env
PROXY=http://адреса:порт
```

**Приклад:**
```env
PROXY=http://18.199.183.77:49232
```

### Робота без проксі:

Якщо працюєш локально або маєш окремий токен - закоментуй або видали рядок:
```env
# PROXY=http://18.199.183.77:49232
```

### Які запити йдуть через проксі:

✅ Всі запити до ChatGPT
✅ Розпізнавання голосу (Whisper)
✅ Синтез мовлення (TTS)

❌ Запити до Telegram API (йдуть напряму)

### Перевірка роботи проксі:

Дивись логи при запуску:
```
INFO - Використовується проксі: http://18.199.183.77:49232
INFO - OpenAI використовує проксі: http://18.199.183.77:49232
```

Якщо проксі не працює:
```
ERROR - Помилка OpenAI API: ProxyError
```

## ✨ Особливості

- 🔒 **Безпека:** Токени зберігаються в `.env` та не потрапляють у Git
- 📝 **Логування:** Усі дії записуються у `bot.log`
- 🛡️ **Відмовостійкість:** Обробка помилок API та мережі
- 🎨 **Модульна архітектура:** Чистий та підтримуваний код
- 📊 **Відповідність PEP8:** Код перевірено flake8
- 🌐 **Підтримка проксі:** Можливість роботи через проксі-сервер

## 🧪 Тестування

Після запуску протестуй усі функції:

1. **ChatGPT інтерфейс:**
   - Надішли текстове питання
   - Надішли голосове питання
   - Перевір кнопки "Ще питання" та "Закінчити"

2. **Випадкові факти:**
   - Отримай факт
   - Попроси ще факт через кнопку

3. **Діалог з особистістю:**
   - Обери персонажа
   - Задай кілька питань підряд (перевір збереження контексту)
   - Заверши діалог

4. **Квіз:**
   - Обери тему
   - Відповідай на питання
   - Перевір підрахунок балів
   - Зміни тему

5. **Перекладач:**
   - Переклади текст
   - Переклади голосове повідомлення
   - Зміни мову

6. **Голосовий режим:**
   - Надішли голосове повідомлення поза командами
   - Отримай голосову відповідь

## 🐛 Відомі обмеження

- Голосові повідомлення розпізнаються українською/російською мовами за замовчуванням
- TTS використовує голос "nova" (можна змінити в `utils/openai_helper.py`)
- Використовується бюджетна модель `gpt-4o-mini`
- При роботі через проксі можлива невелика затримка

## 🚨 Вирішення проблем

### Проблема: "TELEGRAM_TOKEN не знайдено"
**Рішення:** Перевір що файл `.env` створений і містить токен

### Проблема: "Connection timeout" або "ProxyError"
**Рішення:** 
- Перевір адресу проксі в `.env`
- Спробуй без проксі (закоментуй рядок `PROXY`)
- Перевір чи проксі-сервер працює

### Проблема: "Module 'httpx' not found"
**Рішення:** Встанови залежності: `pip install -r requirements.txt`

### Проблема: Бот не відповідає
**Рішення:**
- Перевір чи бот запущений
- Перевір логи в `bot.log`
- Перевір чи правильний токен Telegram

### Проблема: Повільна робота
**Причина:** Проксі-сервер може бути повільним
**Рішення:** Спробуй інший проксі або працюй без проксі локально

## 🌐 Деплой проєкту

### Локальний запуск

Бот автоматично працює в режимі **polling** (постійне опитування Telegram).

Просто запусти:
```bash
python bot.py
```

У логах побачиш:
```
INFO - 🔄 Режим POLLING (локально або інша платформа)
```

### Деплой на Render.com (безкоштовний хостинг)

#### ⚠️ Важливі обмеження Free плану:

- ✅ 750 годин на місяць (достатньо для роботи 24/7)
- ⚠️ **Засинає після 15 хвилин неактивності**
- ⏰ **Холодний старт ~1-2 хвилини** при першому повідомленні після сну
- ⚡ Після пробудження працює швидко

**Це означає:** Якщо ніхто не користується ботом 15 хвилин → він засинає. При новому повідомленні Render будить його (~1-2 хв затримки). Далі працює нормально.

#### Покрокова інструкція:

**1. Створи акаунт на Render**
- Йди на https://render.com
- Зареєструйся через GitHub
- Дозволь доступ до репозиторіїв

**2. Створи Web Service**
- На дашборді натисни **"New +"** → **"Web Service"**
- Підключи свій GitHub репозиторій
- Налаштування:
```
  Name: telegram-gpt-bot (або своє)
  Region: Frankfurt
  Branch: main або master
  Runtime: Python 3
  Build Command: pip install -r requirements.txt
  Start Command: python bot.py
  Instance Type: Free
```

**3. Додай змінні оточення**

У розділі "Environment Variables" додай:
```
TELEGRAM_TOKEN = твій_токен_від_BotFather
OPENAI_TOKEN = твій_токен_OpenAI
PROXY = http://18.199.183.77:49232
```

**4. Додай URL для webhook**

⚠️ **Важливо:** Спочатку створи сервіс БЕЗ цієї змінної!

Після першого деплою Render покаже URL:
```
https://твій-сервіс-xxxx.onrender.com
```

Тоді додай ще одну змінну:
```
RENDER_EXTERNAL_URL = https://твій-сервіс-xxxx.onrender.com
```

Render автоматично перезапустить з webhook режимом.

**5. Перевір логи**

У вкладці "Logs" побачиш:
```
INFO - 🌐 Режим WEBHOOK для Render: https://твій-сервіс.onrender.com
==> Your service is live 🎉
```

**6. Тестуй!**

Напиши боту в Telegram. Перше повідомлення може зайняти 1-2 хв (холодний старт). Далі все працює швидко! ✅

#### Автоматичне оновлення

При `git push` в GitHub:
- Render автоматично помітить зміни
- Перебудує проєкт  
- Перезапустить бота

Дуже зручно для розробки! ✨

### Деплой на інші платформи

**Код універсальний!** Автоматично визначає платформу:

#### Render.com:
- Встанови змінну `RENDER_EXTERNAL_URL`
- Бот використає **webhook** ✅

#### Railway.app / Fly.io / Heroku / VPS:
- **НЕ встановлюй** змінну `RENDER_EXTERNAL_URL`
- Бот використає **polling** ✅

#### Порівняння режимів

| Режим | Коли використовується | Як налаштувати |
|-------|----------------------|----------------|
| **Webhook** | Render.com | Додай `RENDER_EXTERNAL_URL` |
| **Polling** | Всі інші платформи | Нічого не треба |

**У логах побачиш який режим активний:**
- Webhook: `INFO - 🌐 Режим WEBHOOK для Render: ...`
- Polling: `INFO - 🔄 Режим POLLING (локально або інша платформа)`

### Вирішення проблем на Render

#### Бот не відповідає

**Перевір:**
1. Змінні оточення (всі 4: TOKEN, OPENAI, PROXY, RENDER_EXTERNAL_URL)
2. URL в `RENDER_EXTERNAL_URL` правильний
3. Логи - чи є помилки

#### "RuntimeError: To use start_webhook..."

**Рішення:** 
Переконайся що `requirements.txt` містить:
```
python-telegram-bot[webhooks]==22.5
```

#### Повільна перша відповідь

**Це нормально для Free плану!**
- Перше повідомлення після сну: 1-2 хв
- Наступні: швидко

**Варіанти:**
- Оновись до платного ($7/міс) - не засинає
- Або змиріся з затримкою (OK для навчальних проєктів)

### Поради

1. **Попереджай користувачів** про можливу затримку
2. **Моніторь використання** на Render (750 год/міс)
3. **Після здачі проєкту** можна зупинити сервіс (заощадити години)
4. **Для продакшену** розглянь платний план або іншу платформу

### Висновок про Render

**Переваги:**
- ✅ Безкоштовно (750 год/міс)
- ✅ Автодеплой з GitHub
- ✅ Простий у налаштуванні
- ✅ Ідеально для навчання

**Мінуси:**
- ❌ Засинає після 15 хв
- ❌ Холодний старт 1-2 хв
- ❌ Не для продакшену з високим навантаженням

**Підходить для:** Навчальних проєктів, портфоліо, демонстрацій.

## 📝 Ліцензія

MIT License

## 👤 Автор

Створено в рамках навчального проєкту з розробки Telegram-ботів з використанням OpenAI API.

**Додаткова технічна документація:** Детальний опис всіх файлів та функцій проєкту знаходиться в окремому файлі технічної документації.

---

**Якщо виникли питання:**
1. Перевір що всі токени правильно вказані в `.env`
2. Переконайся що встановлені всі залежності з `requirements.txt`
3. Перевір логи у файлі `bot.log`
4. Перевір чи працює проксі (якщо використовується)

**Успішного використання! 🚀**
